services:
  mini-kafka-platform:
    build: .
    ports:
      - "8080:8080"
    environment:
      MINI_KAFKA_STORAGE_DIR: /app/data
    volumes:
      - mini-kafka-data:/app/data

volumes:
  mini-kafka-data:
